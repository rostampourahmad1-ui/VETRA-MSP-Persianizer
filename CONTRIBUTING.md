# راهنمای مشارکت

## پیش از تغییر

ابتدا دامنه تغییر را با `docs/requirements-traceability.md` تطبیق دهید. هر تغییر باید مشخص کند کدام نیازمندی، ماژول و سناریوی پذیرش را پوشش می‌دهد.

## قواعد فنی

تغییرات مربوط به MSP باید با فایل مرجع MPP آزمایش شوند. منطق تقویم و تبدیل تاریخ باید مستقل از UI و قابل تست واحد باشد. متن ترجمه‌شده باید در واژه‌نامه داده‌محور قرار گیرد. هیچ تغییر محاسباتی در موتور MSP بدون تصمیم معماری مکتوب پذیرفته نیست.

## Pull Request

عنوان Pull Request باید دامنه را روشن کند. توضیحات باید شامل مسئله، راه‌حل، ریسک سازگاری، تست‌های اجراشده و تصاویر قبل/بعد برای تغییرات RTL یا View باشد. تغییرات بزرگ باید به کارهای کوچک و قابل بازبینی تقسیم شوند.
